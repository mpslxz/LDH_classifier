import os
import cv2
import numpy as np
from extractFeatures import features


if __name__ == "__main__":
    _ind = np.random.permutation(575)
    train_ind = _ind[:400]
    test_ind = _ind[400:]

    img = cv2.imread('train_data/newLam/img_1.jpg', 0)
    print img
    # lam = []
    # for i in train_ind:
        
    
